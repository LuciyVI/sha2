#include <iostream>
#include <string>
#include <cstring>
#include <vector>
#include </home/dr3ad/repos/LychiVI/sha256/func.h>
#include </home/dr3ad/repos/LychiVI/sha256/sha256.h>


int main()
{
get_file_to_function();
sha256_buff(data);
void sha256_init(struct sha256_buff* buff);
const char* str = "asldkjaslkfjalfsj_121";
char hash[65] = {0};
void sha256_read(const struct sha256_buff* buff, uint8_t* hash);
void sha256_init(struct sha256_buff* buff);

sha256_easy_hash_hex(str, strlen(str), hash);
printf("%s\n", hash);

}
